{
  "name": "aqualityguy",
  "version": "1.0.0",
  "type": "module",
  "description": "AqualityGuy website",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "build:check": "tsc --noEmit",
    "preview": "vite preview",
    "clean": "rm -rf dist",
    "type-check": "tsc --noEmit",
    "deploy": "npm run build && vercel --prod",
    "deploy:preview": "npm run build && vercel",
    "vercel:build": "npm run build"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vite": "^5.4.2"
  },
  "dependencies": {
    "chart.js": "^4.4.0",
    "@supabase/supabase-js": "^2.39.0"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vite": "^5.4.2",
    "@types/node": "^20.10.0",
    "@vercel/node": "^3.0.0"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  }
}
